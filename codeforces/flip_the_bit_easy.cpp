#include <bits/stdc++.h>



int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int t;

	std::cin >> t;

	while(t--)
	{
		int n, k, p, ans = 0;

		std::cin >> n >> k;

		std::vector<int> arr(n);

		for(auto &i : arr) std::cin >> i ;

		std::cin >> p;

	}
}	
